<% @articles.each do |article| %>
  <!--<div class="card my-3 border-0">
    <div class="card-body">
      <h3 class="inline-block card-title border-bottom py-2"><%= link_to article.title, article_path(article), class: "article-title" %></h3>
      <p class="card-text"><%= article.content %></p>
    </div>
  </div>-->

  <div class="card my-3">
    <div class="card-body p-2">
      <h6 class="card-title"><%= link_to article.title, article_path(article), class: "article-title" %></h6>
      <p class="card-text">By. <%=link_to "@#{article.user.to_param}", user_path(article.user.to_param) %></p>

      <p class="card-text"><%= article.content[0..20] %></p>
      <!--<a href="#" class="btn btn-primary">Go somewhere</a> -->
    </div>
  </div>
  
<% end %>